FROM python:3
RUN apt-get update && apt-get install -y curl
RUN pip install requests flask werkzeug gunicorn flask_mongoengine flask_admin flask_login flask_bcrypt pyOpenSSL Flask-Markdown psutil gevent
WORKDIR /analyzer
ADD ./ .